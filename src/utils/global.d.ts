// declare global {
//   interface String {
//     opacity(opacity: number): string;
//   }
// }

// function opacity(this: string, opacity: number): string {
//   return `rgba(${this}, ${opacity})`;
// }

// String.prototype.opacity = opacity;
